// Utility for tracking and retrieving page views
// Static build: page view tracking is disabled.

// Function to increment view count for a post
export async function incrementPageView() {
  // Static builds do not support server-side page view tracking.
}

// Function to get popular posts
export async function getPopularPosts() {
  // Static builds do not support server-side popular post tracking.
  return [];
}
