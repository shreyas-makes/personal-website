# AGENTS.md

## Design System Workflow For AI Sessions

Before writing or modifying any UI code, read the relevant spec file in `specs/`. Use only tokens from `src/styles/tokens.css`. Run the token audit script before committing. Zero errors required.

```bash
npm run token:audit
```
